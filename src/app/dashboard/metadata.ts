import type { Metadata } from 'next';
import { defaultViewport } from '../metadata';

export const viewport = defaultViewport;

export const metadata: Metadata = {
    title: 'Dashboard',
    description: 'Manage your attendance registers and employees',
}; 